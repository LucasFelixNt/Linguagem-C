#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void){

	int num;

	printf("Digite um numero: \n");
	scanf("%d", %num);

	num >= 0 ? printf("Numero positivo \n"); : printf("Numero negativo \n");

	
	system(pause);
	return(0);
	
}