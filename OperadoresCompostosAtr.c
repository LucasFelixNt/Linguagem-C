#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void){

	// += num = num + x
	// -= num = num - x
	// *= num = num * x
	// /= num = num / x

	
	system(pause);
	return(0);
	
}